import { GridSelectableSettings } from '../interfaces/GridSelectableSettings';
import { GridDragSelectionReleaseEvent } from '../interfaces/events';
/** @hidden */
export declare const getSelectionOptions: (selectable?: GridSelectableSettings) => {
    enabled: boolean;
    drag: boolean;
    mode: "multiple" | "single";
    cell: boolean;
};
/** @hidden */
export interface GridSelectionProps {
    children: any;
    selectable?: GridSelectableSettings;
    onRelease: (options: GridDragSelectionReleaseEvent) => void;
}
/** @hidden */
export declare const GridSelection: (props: GridSelectionProps) => any;
